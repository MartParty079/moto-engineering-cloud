import { supabase } from './supabase.js';

const $=q=>document.querySelector(q);
const set=(q,v)=>{const e=$(q);if(e)e.textContent=v};
let mounted=false,mounting=false,motionEnabled=false,lastRoadAt=0,lastRoadPos=null,lastHeading=null,lastWeatherAt=0,roadBusy=false,weatherBusy=false;
let leanZero=0,lean=null,pitch=null,accelG=null;

const mph=mps=>Number.isFinite(mps)?mps*2.236936:null;
const hav=(a,b)=>{if(!a||!b)return Infinity;const R=3958.7613,r=x=>x*Math.PI/180,dLat=r(b.lat-a.lat),dLon=r(b.lon-a.lon),q=Math.sin(dLat/2)**2+Math.cos(r(a.lat))*Math.cos(r(b.lat))*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(q))};
const angleDiff=(a,b)=>!Number.isFinite(a)||!Number.isFinite(b)?Infinity:Math.abs(((a-b+540)%360)-180);
const pos=(fresh=false)=>new Promise((ok,no)=>navigator.geolocation?.getCurrentPosition(p=>ok({lat:p.coords.latitude,lon:p.coords.longitude,heading:Number.isFinite(p.coords.heading)?p.coords.heading:null,speed:mph(p.coords.speed),accuracy:p.coords.accuracy}),no,{enableHighAccuracy:true,maximumAge:fresh?0:5000,timeout:15000})||no(new Error('GPS unavailable')));
const activeBikeName=()=>$('.liveBikeHero h3')?.textContent.trim()||'';

function panel(title,id,body){return `<details class="rideSafePanel" ${id==='safeSensors'?'open':''}><summary><b>${title}</b><span id="${id}Badge">READY</span></summary><div id="${id}" class="rideSafePanelBody">${body}</div></details>`}

function markup(){return `<section id="rideSafeEnhancements" class="rideSafeEnhancements"><div class="rideSafeEnhanceHead"><div><small>SAFE ADD-ONS</small><h3>Ride intelligence</h3></div><span>CORE LOGGER ISOLATED</span></div>
${panel('Sensors & lean','safeSensors',`<div class="rideSafeGrid"><article><small>LEAN</small><strong id="safeLean">--°</strong></article><article><small>PITCH</small><strong id="safePitch">--°</strong></article><article><small>ACCELERATION</small><strong id="safeAccel">-- g</strong></article></div><div class="rideSafeActions"><button id="enableRideSensors">ENABLE SENSORS</button><button id="zeroRideLean">ZERO LEAN</button></div><p id="safeSensorStatus">Sensors are off until enabled.</p>`)}
${panel('Local weather','safeWeather',`<div class="rideSafeGrid"><article><small>TEMP</small><strong id="safeTemp">--°</strong></article><article><small>RAIN</small><strong id="safeRain">--%</strong></article><article><small>HUMIDITY</small><strong id="safeHumidity">--%</strong></article><article><small>WIND</small><strong id="safeWind">--</strong></article><article><small>SUNRISE</small><strong id="safeSunrise">--</strong></article><article><small>SUNSET</small><strong id="safeSunset">--</strong></article></div><button id="refreshSafeWeather">REFRESH WEATHER</button><p id="safeWeatherStatus">Waiting to update.</p>`)}
${panel('Road & speed limit','safeRoad',`<div class="rideSafeRoad"><div><small>ROAD</small><strong id="safeRoadName">Waiting for lookup</strong><span id="safeRoadSource">—</span></div><div class="safeLimit"><small>LIMIT</small><strong id="safeRoadLimit">--</strong></div></div><label>Provider<select id="safeRoadProvider"><option value="osm">OpenStreetMap</option><option value="tomtom">TomTom</option><option value="google">Google Roads</option><option value="auto">Automatic</option></select></label><button id="refreshSafeRoad">REFRESH ROAD</button><p id="safeRoadStatus">Hybrid lookup starts after the logger is stable.</p>`)}
${panel('Ride tools','safeTools',`<div class="rideSafeGrid"><article><small>EST. RANGE</small><strong id="safeRange">—</strong><span id="safeMpg">Fuel data needed</span></article><article><small>NEXT SERVICE</small><strong id="safeService">—</strong><span id="safeServiceSub">No interval</span></article><article><small>ACTIVE TIRES</small><strong id="safeTires">—</strong><span id="safeTiresSub">No profile</span></article></div><div class="rideSafeActions"><button id="safeVoiceNote">VOICE NOTE</button><button id="safeMarkRoad">MARK ROAD</button><button id="refreshSafeTools">REFRESH TOOLS</button></div><p id="safeToolsStatus">Tools load independently from ride logging.</p>`)}
</section>`}

async function enableSensors(){
 try{
  let ok=true;
  if(typeof DeviceMotionEvent!=='undefined'&&typeof DeviceMotionEvent.requestPermission==='function')ok=(await DeviceMotionEvent.requestPermission())==='granted';
  if(ok&&typeof DeviceOrientationEvent!=='undefined'&&typeof DeviceOrientationEvent.requestPermission==='function')ok=(await DeviceOrientationEvent.requestPermission())==='granted';
  if(!ok)throw new Error('Sensor permission was not granted.');
  if(!motionEnabled){window.addEventListener('deviceorientation',onOrientation,{passive:true});window.addEventListener('devicemotion',onMotion,{passive:true});motionEnabled=true}
  set('#safeSensorStatus','Sensors active. Use ZERO LEAN with the phone mounted upright.');set('#safeSensorsBadge','ACTIVE');
 }catch(e){set('#safeSensorStatus',e.message||String(e));set('#safeSensorsBadge','OFF')}
}
function onOrientation(e){if(!motionEnabled)return;lean=Number.isFinite(e.gamma)?e.gamma-leanZero:null;pitch=Number.isFinite(e.beta)?e.beta:null;set('#safeLean',Number.isFinite(lean)?`${lean.toFixed(1)}°`:'--°');set('#safePitch',Number.isFinite(pitch)?`${pitch.toFixed(1)}°`:'--°')}
function onMotion(e){if(!motionEnabled)return;const a=e.accelerationIncludingGravity||e.acceleration||{};if([a.x,a.y,a.z].every(Number.isFinite)){accelG=Math.sqrt(a.x*a.x+a.y*a.y+a.z*a.z)/9.80665;set('#safeAccel',`${accelG.toFixed(2)} g`)}}

async function refreshWeather(force=false){if(weatherBusy||(!force&&Date.now()-lastWeatherAt<600000))return;weatherBusy=true;set('#safeWeatherStatus','Updating weather…');try{const p=await pos(force),u=new URLSearchParams({latitude:p.lat,longitude:p.lon,current:'temperature_2m,relative_humidity_2m,precipitation_probability,wind_speed_10m,wind_direction_10m',daily:'sunrise,sunset',temperature_unit:'fahrenheit',wind_speed_unit:'mph',timezone:'auto',forecast_days:'1'}),r=await fetch(`https://api.open-meteo.com/v1/forecast?${u}`);if(!r.ok)throw new Error(`Weather HTTP ${r.status}`);const d=await r.json(),c=d.current||{},day=d.daily||{};set('#safeTemp',Number.isFinite(Number(c.temperature_2m))?`${Math.round(c.temperature_2m)}°`:'--°');set('#safeRain',Number.isFinite(Number(c.precipitation_probability))?`${Math.round(c.precipitation_probability)}%`:'--%');set('#safeHumidity',Number.isFinite(Number(c.relative_humidity_2m))?`${Math.round(c.relative_humidity_2m)}%`:'--%');set('#safeWind',Number.isFinite(Number(c.wind_speed_10m))?`${Math.round(c.wind_speed_10m)} mph`:'--');set('#safeSunrise',day.sunrise?.[0]?new Date(day.sunrise[0]).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}):'--');set('#safeSunset',day.sunset?.[0]?new Date(day.sunset[0]).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}):'--');lastWeatherAt=Date.now();set('#safeWeatherStatus',`Updated ${new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}`);set('#safeWeatherBadge','LIVE')}catch(e){set('#safeWeatherStatus',e.message||String(e));set('#safeWeatherBadge','ERROR')}finally{weatherBusy=false}}

async function refreshRoad(force=false,reason='manual'){if(roadBusy)return;roadBusy=true;set('#safeRoadStatus',`Road lookup: ${reason}`);try{const p=await pos(force),provider=$('#safeRoadProvider')?.value||'osm',params=new URLSearchParams({lat:p.lat,lon:p.lon,provider});if(Number.isFinite(p.heading))params.set('heading',p.heading);if(Number.isFinite(p.speed))params.set('speed',p.speed);if(lastRoadPos){params.set('prevLat',lastRoadPos.lat);params.set('prevLon',lastRoadPos.lon)}const {data:{session}}=await supabase.auth.getSession(),headers={Accept:'application/json'};if(session?.access_token)headers.Authorization=`Bearer ${session.access_token}`;const r=await fetch(`/api/road-info?${params}`,{headers});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Road HTTP ${r.status}`);lastRoadAt=Date.now();lastRoadPos=p;lastHeading=p.heading;set('#safeRoadName',d.road||'Road not identified');set('#safeRoadLimit',d.limit?.mph??d.limit?.display??'--');set('#safeRoadSource',d.source||'MotoCloud');set('#safeRoadStatus',d.diagnostic||'Road lookup complete');set('#safeRoadBadge','LIVE')}catch(e){set('#safeRoadStatus',e.message||String(e));set('#safeRoadBadge','ERROR')}finally{roadBusy=false}}
async function roadMonitor(){if(!mounted||!$('#rideSafeEnhancements'))return;try{const p=await pos(false),moved=hav(lastRoadPos,p),turned=angleDiff(lastHeading,p.heading),age=Date.now()-lastRoadAt;let reason=null;if(!lastRoadAt)reason='initial';else if(moved>=0.08)reason='moved 400+ ft';else if(turned>=30)reason='significant turn';else if(age>=180000)reason='3-minute refresh';if(reason)await refreshRoad(false,reason)}catch{}}

async function refreshTools(){set('#safeToolsStatus','Loading tools…');try{const {data:{session}}=await supabase.auth.getSession();if(!session)throw new Error('Not signed in');const name=activeBikeName(),{data:bikes}=await supabase.from('bikes').select('*'),bike=(bikes||[]).find(b=>[b.year,b.make,b.model].filter(Boolean).join(' ')===name||b.name===name);if(!bike)throw new Error('Active bike not found');const [f,m,t,r]=await Promise.all([supabase.from('fuel_entries').select('*').eq('bike_id',bike.id).order('odometer_miles'),supabase.from('maintenance_intervals').select('*').eq('bike_id',bike.id).eq('enabled',true),supabase.from('tire_profiles').select('*').eq('bike_id',bike.id).eq('active',true).limit(1).maybeSingle(),supabase.from('ride_sessions').select('duration_seconds').eq('bike_id',bike.id).eq('status','complete')]);const fuel=f.data||[],full=fuel.filter(x=>x.full_tank);let mi=0,gal=0;for(let i=1;i<full.length;i++){const d=Number(full[i].odometer_miles)-Number(full[i-1].odometer_miles);if(d>0&&d<2000){mi+=d;gal+=Number(full[i].gallons||0)}}const mpg=gal?mi/gal:null;set('#safeMpg',mpg?`${mpg.toFixed(1)} mpg`:'Need two full fill-ups');set('#safeRange',mpg?`${Math.round(mpg*2)} mi`:'—');const odo=Number(bike.odometer||0),hrs=(r.data||[]).reduce((a,x)=>a+Number(x.duration_seconds||0),0)/3600,due=(m.data||[]).map(x=>({x,rem:Number.isFinite(Number(x.interval_miles))?Number(x.interval_miles)-(odo-Number(x.last_service_miles||0)):Number.isFinite(Number(x.interval_hours))?Number(x.interval_hours)-(hrs-Number(x.last_service_hours||0)):Infinity})).sort((a,b)=>a.rem-b.rem)[0];set('#safeService',due?.x?.item_name||'—');set('#safeServiceSub',Number.isFinite(due?.rem)?`${Math.round(due.rem)} remaining`:'No interval');set('#safeTires',t.data?.name||'—');set('#safeTiresSub',t.data?'Active profile':'No profile');set('#safeToolsStatus',`${name} tools current`);set('#safeToolsBadge','LIVE')}catch(e){set('#safeToolsStatus',e.message||String(e));set('#safeToolsBadge','ERROR')}}

async function saveNote(){const text=prompt('Ride note:');if(!text)return;try{const p=await pos(),{data:{session}}=await supabase.auth.getSession();await supabase.from('ride_notes').insert({user_id:session.user.id,note_text:text,latitude:p.lat,longitude:p.lon});set('#safeToolsStatus','Ride note saved')}catch(e){set('#safeToolsStatus',e.message||String(e))}}
async function markRoad(){const type=prompt('Road condition (gravel, pothole, construction, water, police, accident, curve, trail):','gravel');if(!type)return;try{const p=await pos(),{data:{session}}=await supabase.auth.getSession();await supabase.from('road_condition_tags').insert({user_id:session.user.id,tag_type:type,latitude:p.lat,longitude:p.lon,road_name:$('#safeRoadName')?.textContent||null});set('#safeToolsStatus',`${type} marked`)}catch(e){set('#safeToolsStatus',e.message||String(e))}}

async function mount(){if(mounted||mounting||!$('#rideStop')||$('#rideSafeEnhancements'))return;mounting=true;try{await new Promise(r=>setTimeout(r,1800));if(!$('#rideStop')||$('#rideSafeEnhancements'))return;const body=$('#rideCenterBody');if(!body)return;body.insertAdjacentHTML('beforeend',markup());mounted=true;$('#enableRideSensors').onclick=enableSensors;$('#zeroRideLean').onclick=()=>{if(Number.isFinite(lean))leanZero+=lean;set('#safeSensorStatus','Lean zeroed')};$('#refreshSafeWeather').onclick=()=>refreshWeather(true);$('#refreshSafeRoad').onclick=()=>refreshRoad(true,'manual');$('#refreshSafeTools').onclick=refreshTools;$('#safeVoiceNote').onclick=saveNote;$('#safeMarkRoad').onclick=markRoad;setTimeout(()=>refreshWeather(false),1200);setTimeout(()=>refreshRoad(false,'ride stabilized'),2200);setTimeout(refreshTools,3200);window.__safeRoadTimer=setInterval(roadMonitor,15000)}finally{mounting=false}}

const observer=new MutationObserver(()=>queueMicrotask(mount));observer.observe(document.body,{childList:true,subtree:true});
window.addEventListener('pagehide',()=>{if(window.__safeRoadTimer)clearInterval(window.__safeRoadTimer);window.removeEventListener('deviceorientation',onOrientation);window.removeEventListener('devicemotion',onMotion)});
mount();
