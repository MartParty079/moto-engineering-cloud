name: Dev audit

on:
  push:
    branches: [dev]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dev-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Verify durable journal and copied database contract
        run: npm test
      - name: Check JavaScript syntax and common mistakes
        shell: bash
        run: |
          set -o pipefail
          npm run check:syntax 2>&1 | tee audit.log
      - name: Audit buttons, routes, and interaction wiring
        shell: bash
        run: |
          set -o pipefail
          npm run check:interactions 2>&1 | tee interaction-audit.log
      - name: Audit recording architecture
        shell: bash
        run: |
          set -o pipefail
          node scripts/recording-static-audit.mjs 2>&1 | tee recording-static-audit.log
      - name: Build production bundle
        shell: bash
        run: |
          set -o pipefail
          npm run build 2>&1 | tee build.log
      - name: Upload audit output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dev-audit-output
          path: |
            audit.log
            interaction-audit.log
            recording-static-audit.log
            recording-static-audit.json
            recording-static-audit.md
            build.log
          if-no-files-found: ignore
          retention-days: 7

  browser-evidence:
    needs: audit
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install app dependencies
        run: npm ci --no-audit --no-fund
      - name: Install Playwright browser runners
        run: |
          npm install --no-save --package-lock=false playwright@1.54.2
          npx playwright install --with-deps chromium webkit
      - name: Build production bundle
        run: npm run build
      - name: Start production preview
        shell: bash
        run: |
          VITE_SUPABASE_URL=http://127.0.0.1:54321 VITE_SUPABASE_PUBLISHABLE_KEY=local-test npm run dev -- --host 127.0.0.1 > vite-preview.log 2>&1 &
          echo $! > vite-preview.pid
          for attempt in {1..40}; do
            if curl --fail --silent http://127.0.0.1:5173/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat vite-preview.log
          exit 1
      - name: Verify current shell and durable recovery in both engines
        run: |
          for engine in chromium webkit; do
            E2E_BROWSER=$engine npm run test:browser
            E2E_BROWSER=$engine npm run test:browser:rides
          done
      - name: Upload screenshot and diagnostic proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: moto-mission-browser-proof
          path: |
            test-results/proof/
            vite-preview.log
          if-no-files-found: error
          retention-days: 14
