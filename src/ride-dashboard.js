const $=q=>document.querySelector(q);
const esc=(s='')=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const STORE='motoRideDashLayoutV1';
const defaults=[
 {id:'street',name:'Street',widgets:[['speed','xl'],['road','wide'],['limit','small'],['heading','small'],['weather','wide'],['rideTime','small'],['distance','small']]},
 {id:'adventure',name:'Adventure',widgets:[['map','hero'],['distance','small'],['altitude','small'],['range','small'],['gps','small'],['road','wide']]},
 {id:'performance',name:'Performance',widgets:[['speed','wide'],['maxSpeed','small'],['avgSpeed','small'],['lean','small'],['accel','small'],['heading','small'],['altitude','small']]},
 {id:'navigation',name:'Navigation',widgets:[['map','hero'],['road','wide'],['heading','small'],['limit','small'],['distance','small'],['weather','small']]},
 {id:'bike',name:'Bike',widgets:[['bike','wide'],['distance','small'],['rideTime','small'],['range','small'],['gps','small'],['placeholder','wide']]}
];
const catalog={
 speed:{label:'Speed',cat:'Ride'},distance:{label:'Distance',cat:'Ride'},rideTime:{label:'Ride Time',cat:'Ride'},avgSpeed:{label:'Average Speed',cat:'Ride'},maxSpeed:{label:'Max Speed',cat:'Ride'},
 road:{label:'Road',cat:'Road'},limit:{label:'Speed Limit',cat:'Road'},heading:{label:'Heading',cat:'GPS'},altitude:{label:'Altitude',cat:'GPS'},gps:{label:'GPS Status',cat:'GPS'},
 weather:{label:'Weather',cat:'Weather'},range:{label:'Fuel Range',cat:'Adventure'},map:{label:'Adventure Map',cat:'Adventure'},lean:{label:'Lean',cat:'Performance'},accel:{label:'Acceleration',cat:'Performance'},bike:{label:'Motorcycle',cat:'Bike'},placeholder:{label:'Future ESP Gauges',cat:'Bike'}
};
let layout=load(),edit=false,rideActionBusy=false,state={ride:null,gps:null,road:null,weather:null,lean:null};
function load(){try{const x=JSON.parse(localStorage.getItem(STORE));if(Array.isArray(x)&&x.length)return x}catch{}return structuredClone(defaults)}
function save(){localStorage.setItem(STORE,JSON.stringify(layout))}
function nav(){const n=$('#nav');if(!n||$('#rideDashNav'))return;const b=document.createElement('button');b.id='rideDashNav';b.innerHTML='<span class="navIcon">▦</span><span>Ride Dash</span><em>NEW</em>';b.onclick=open;const g=[...n.querySelectorAll('.navGroup')].find(x=>x.querySelector('.navLabel')?.textContent.trim()==='Operations');(g||n).appendChild(b)}
function open(){close();state.ride=window.MotoRide?.getState?.()||state.ride;const o=document.createElement('div');o.id='rideDashOverlay';o.innerHTML=`<section class="rideDash"><header><div><small>CUSTOM RIDE DISPLAY</small><h2>Ride Dash</h2></div><div><button id="dashAdventure" type="button" title="Open Adventure Mode" aria-label="Open Adventure Mode">ADV</button><button id="dashEdit">EDIT</button><button id="dashClose">×</button></div></header><div class="dashRideControl" id="dashRideControl"><div class="dashRideIdentity"><span id="dashRideDot"></span><div><small id="dashRideStatus">NOT RECORDING</small><strong id="dashRideBike">Select a motorcycle to begin</strong></div></div><button id="dashRideToggle" type="button">START RIDE</button></div><div class="dashTabs" id="dashTabs"></div><div class="dashPages" id="dashPages"></div><footer><button id="dashPrev">‹</button><div id="dashDots"></div><button id="dashAddPage">＋ PAGE</button><button id="dashNext">›</button></footer></section>`;document.body.appendChild(o);$('#dashClose').onclick=close;$('#dashAdventure').onclick=openAdventure;$('#dashEdit').onclick=()=>{edit=!edit;o.classList.toggle('editing',edit);$('#dashEdit').textContent=edit?'DONE':'EDIT';render()};$('#dashRideToggle').onclick=toggleRide;$('#dashAddPage').onclick=addPage;$('#dashPrev').onclick=()=>scrollPage(-1);$('#dashNext').onclick=()=>scrollPage(1);render();refresh()}
function close(){$('#dashRidePicker')?.remove();$('#rideDashOverlay')?.remove()}
function openAdventure(){const adventure=$('#adventureNav');if(!adventure){alert('Adventure Mode is still loading. Try again in a moment.');return}close();requestAnimationFrame(()=>adventure.click())}
function render(){const pages=$('#dashPages'),tabs=$('#dashTabs');if(!pages)return;tabs.innerHTML=layout.map((p,i)=>`<button data-page="${i}">${esc(p.name)}</button>`).join('');pages.innerHTML=layout.map((p,pi)=>`<section class="dashPage" data-page="${pi}"><div class="dashPageHead"><h3>${esc(p.name)}</h3>${edit?`<div><button data-rename="${pi}">RENAME</button><button data-add="${pi}">＋ WIDGET</button>${layout.length>1?`<button data-delete-page="${pi}">DELETE</button>`:''}</div>`:''}</div><div class="dashGrid">${p.widgets.map((w,wi)=>widget(w,pi,wi)).join('')}</div></section>`).join('');tabs.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>pages.children[+b.dataset.page].scrollIntoView({behavior:'smooth',inline:'start'}));pages.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>picker(+b.dataset.add));pages.querySelectorAll('[data-rename]').forEach(b=>b.onclick=()=>rename(+b.dataset.rename));pages.querySelectorAll('[data-delete-page]').forEach(b=>b.onclick=()=>deletePage(+b.dataset.deletePage));pages.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>removeWidget(+b.dataset.p,+b.dataset.w));pages.querySelectorAll('[data-size]').forEach(b=>b.onclick=()=>resizeWidget(+b.dataset.p,+b.dataset.w));pages.querySelectorAll('[data-left]').forEach(b=>b.onclick=()=>moveWidget(+b.dataset.p,+b.dataset.w,-1));pages.querySelectorAll('[data-right]').forEach(b=>b.onclick=()=>moveWidget(+b.dataset.p,+b.dataset.w,1));refresh();dots()}
function widget(w,pi,wi){const [type,size='small']=w,c=catalog[type]||{label:type};return `<article class="dashWidget size-${size}" data-widget="${type}"><small>${esc(c.label)}</small><div class="dashValue" data-value="${type}">--</div>${edit?`<div class="widgetEdit"><button data-left data-p="${pi}" data-w="${wi}">←</button><button data-size data-p="${pi}" data-w="${wi}">SIZE</button><button data-right data-p="${pi}" data-w="${wi}">→</button><button data-remove data-p="${pi}" data-w="${wi}">×</button></div>`:''}</article>`}
function picker(pi){$('#dashPicker')?.remove();const m=document.createElement('div');m.id='dashPicker';m.className='dashPicker';const groups={};Object.entries(catalog).forEach(([k,v])=>(groups[v.cat]??=[]).push([k,v]));m.innerHTML=`<section><header><div><small>WIDGET LIBRARY</small><h3>Add to ${esc(layout[pi].name)}</h3></div><button>×</button></header>${Object.entries(groups).map(([g,items])=>`<h4>${esc(g)}</h4><div>${items.map(([k,v])=>`<button data-type="${k}">${esc(v.label)}</button>`).join('')}</div>`).join('')}</section>`;document.body.appendChild(m);m.querySelector('header button').onclick=()=>m.remove();m.querySelectorAll('[data-type]').forEach(b=>b.onclick=()=>{layout[pi].widgets.push([b.dataset.type,b.dataset.type==='map'?'hero':'small']);save();m.remove();render()})}
function showRidePicker(){if(rideActionBusy)return;const controller=window.MotoRide;if(!controller){alert('Ride logger is still loading. Try again in a moment.');return}const bikes=controller.getBikes?.()||[];$('#dashRidePicker')?.remove();const m=document.createElement('div');m.id='dashRidePicker';m.className='dashRidePicker';m.innerHTML=`<section><header><div><small>START RIDE</small><h3>Select motorcycle</h3></div><button id="dashRidePickerClose" type="button">×</button></header><div class="dashBikeGrid">${bikes.map(b=>`<button type="button" data-bike-id="${esc(b.id)}"><span><strong>${esc(b.name)}</strong><small>${Math.round(Number(b.odometer||0))} mi</small></span><b>START</b></button>`).join('')||'<p>No motorcycles found. Add one in Garage first.</p>'}</div></section>`;document.body.appendChild(m);$('#dashRidePickerClose').onclick=()=>m.remove();m.querySelectorAll('[data-bike-id]').forEach(b=>b.onclick=()=>startRide(b.dataset.bikeId,m))}
async function startRide(bikeId,modal){if(rideActionBusy)return;rideActionBusy=true;modal?.querySelectorAll('button').forEach(b=>b.disabled=true);state.ride={...(state.ride||{}),starting:true};refresh();try{await window.MotoRide.start(bikeId);modal?.remove()}catch(error){alert(error?.message||String(error))}finally{rideActionBusy=false;state.ride=window.MotoRide?.getState?.()||state.ride;refresh()}}
async function toggleRide(){if(rideActionBusy)return;const controller=window.MotoRide;if(!controller){alert('Ride logger is still loading. Try again in a moment.');return}const ride=controller.getState?.()||state.ride||{};if(!ride.active){showRidePicker();return}if(!confirm('Stop and save this ride?'))return;rideActionBusy=true;refresh();try{await controller.stop()}catch(error){alert(error?.message||String(error))}finally{rideActionBusy=false;state.ride=controller.getState?.()||state.ride;refresh()}}
function renderRideControl(){const ride=state.ride||window.MotoRide?.getState?.()||{},control=$('#dashRideControl'),status=$('#dashRideStatus'),bike=$('#dashRideBike'),toggle=$('#dashRideToggle'),dot=$('#dashRideDot');if(!control||!status||!bike||!toggle)return;const active=Boolean(ride.active),starting=Boolean(ride.starting);control.classList.toggle('recording',active);control.classList.toggle('starting',starting||rideActionBusy);dot.classList.toggle('live',active);status.textContent=active?'RECORDING':starting?'STARTING LOGGER':'NOT RECORDING';bike.textContent=active?(ride.bikeName||'Motorcycle'):(starting?'Preparing GPS and ride storage…':'Select a motorcycle to begin');toggle.disabled=starting||rideActionBusy;toggle.textContent=active?(rideActionBusy?'SAVING…':'STOP & SAVE'):(starting||rideActionBusy?'STARTING…':'START RIDE')}
function rename(i){const n=prompt('Page name',layout[i].name);if(n?.trim()){layout[i].name=n.trim().slice(0,24);save();render()}}
function addPage(){layout.push({id:`custom-${Date.now()}`,name:`Custom ${layout.length+1}`,widgets:[['speed','wide'],['distance','small'],['heading','small']]});save();render();setTimeout(()=>$('#dashPages')?.lastElementChild?.scrollIntoView({behavior:'smooth',inline:'start'}),50)}
function deletePage(i){if(confirm(`Delete ${layout[i].name}?`)){layout.splice(i,1);save();render()}}
function removeWidget(p,w){layout[p].widgets.splice(w,1);save();render()}
function moveWidget(p,w,d){const a=layout[p].widgets,j=w+d;if(j<0||j>=a.length)return;[a[w],a[j]]=[a[j],a[w]];save();render()}
function resizeWidget(p,w){const sizes=['small','wide','xl','hero'],x=layout[p].widgets[w];x[1]=sizes[(sizes.indexOf(x[1])+1)%sizes.length];save();render()}
function scrollPage(d){const p=$('#dashPages');p?.scrollBy({left:d*p.clientWidth,behavior:'smooth'})}
function dots(){const p=$('#dashPages'),d=$('#dashDots');if(!p||!d)return;d.innerHTML=layout.map((_,i)=>`<i class="${i===0?'active':''}"></i>`).join('');p.onscroll=()=>{const i=Math.round(p.scrollLeft/Math.max(1,p.clientWidth));[...d.children].forEach((x,j)=>x.classList.toggle('active',i===j));[...$('#dashTabs').children].forEach((x,j)=>x.classList.toggle('active',i===j))}}
function value(type){const r=state.ride||{},g=state.gps||{},road=state.road||{},w=state.weather||{};switch(type){case'speed':return `${Math.round(r.speedMph??g.speed??0)} <span>mph</span>`;case'distance':return `${Number(r.distanceMiles??0).toFixed(2)} <span>mi</span>`;case'rideTime':return r.elapsedText||'00:00:00';case'avgSpeed':return `${Math.round(r.averageSpeedMph??0)} <span>mph</span>`;case'maxSpeed':return `${Math.round(r.maxSpeedMph??0)} <span>mph</span>`;case'road':return esc(road.road||'Waiting for road');case'limit':return Number.isFinite(road.limit_mph)?`${road.limit_mph} <span>mph</span>`:'--';case'heading':return Number.isFinite(r.heading??g.heading)?`${Math.round(r.heading??g.heading)}°`:'--°';case'altitude':return Number.isFinite(r.altitudeFt)?`${Math.round(r.altitudeFt)} <span>ft</span>`:Number.isFinite(g.altitude)?`${Math.round(g.altitude*3.28084)} <span>ft</span>`:'--';case'gps':return r.gpsLocked?`LOCKED <span>${Number.isFinite(r.accuracyFt)?`±${Math.round(r.accuracyFt)} ft`:''}</span>`:Number.isFinite(g.latitude)?`LOCKED <span>±${Math.round((g.accuracy||0)*3.28084)} ft</span>`:'WAITING';case'weather':return `${w.temperature??w.temp??'--'}° <span>${w.rainChance??w.rain??'--'}% rain</span>`;case'range':return `${localStorage.getItem('motoEstimatedRange')||'--'} <span>mi</span>`;case'lean':return `${Math.round(state.lean?.lean||0)}°`;case'accel':return `${Number(state.lean?.accel||0).toFixed(2)} <span>g</span>`;case'bike':return esc(r.bikeName||'Select a motorcycle');case'map':return '<button class="dashOpenAdventure">OPEN ADVENTURE MAP</button>';case'placeholder':return '<span>RPM · TEMP · VOLTAGE<br>Ready for ESP integration</span>';default:return'--'}}
function refresh(){renderRideControl();document.querySelectorAll('#rideDashOverlay [data-value]').forEach(x=>x.innerHTML=value(x.dataset.value));document.querySelectorAll('.dashOpenAdventure').forEach(b=>b.onclick=openAdventure)}
window.addEventListener('moto-ride-state',e=>{state.ride=e.detail;refresh()});window.addEventListener('moto-gps-fix',e=>{state.gps=e.detail;refresh()});window.addEventListener('moto-road-update',e=>{state.road=e.detail;refresh()});window.addEventListener('moto-weather-update',e=>{state.weather=e.detail;refresh()});window.addEventListener('moto-motion-update',e=>{state.lean=e.detail;refresh()});
const observer=new MutationObserver(nav);observer.observe(document.querySelector('#app')||document.body,{childList:true,subtree:false});nav();
